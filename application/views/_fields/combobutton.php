<div>
    <span class="btn-group" data-toggle="buttons" data-target="#{name}">
        {buttons}
        <button class="btn" value="{value}">{text}</button>
        {/buttons}
    </span>
</div>
<input type="hidden" name="{name}" id="{name}" value="0" />