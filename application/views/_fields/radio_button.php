<div class="control-group">
    <label class="control-label">{label}</label>
    <div class="controls">
        <input type="radiobutton" id="{name}" name="{name}" {checked} {disabled}/>
               <small>{explain}</small>
        <span class="maxsize"> (up to {maxlen} chars)</span>
    </div>
</div>

